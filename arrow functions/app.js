const double = (number) => {
  return nunber * 2
}

// quando só tem um parametro pode retirar os parentese
const double = number => {
  return nunber * 2
}


// quando não tem parametros os parentese são obrigatorio
const double = () => {
  return nunber * 2
}


// quando o bloco de codigo tem so uma linha retornando o valor, pode tirar a palavra chave
// retur e eliminar os parentese.
const double = () => nunber * 2
